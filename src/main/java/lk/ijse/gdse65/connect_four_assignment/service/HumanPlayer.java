package lk.ijse.gdse65.connect_four_assignment.service;

public class HumanPlayer extends Player{

    // implements the logic for human players, allowing them
    // to select a move by specifying a row and column

    public HumanPlayer(Board board) {
        super(board);
    }

    // Implements the abstract method, placing an X on the board at the specified location
    @Override
    public void move(int row, int col) {

    }
}
